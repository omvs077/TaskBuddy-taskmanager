<UserControl x:Class="TaskBuddyWPF.Controls.PerformanceGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="HeaderGrid" Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleText" Grid.Column="0" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock x:Name="ValueText" Grid.Column="1" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </Grid>
        <Border Grid.Row="1" Background="#14FFFFFF" CornerRadius="4" ClipToBounds="True">
            <Canvas x:Name="GraphCanvas"/>
        </Border>
    </Grid>
</UserControl>
